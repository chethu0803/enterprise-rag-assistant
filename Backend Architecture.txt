[Frontend UI]
    |
    v
[API Gateway (FastAPI/Nginx)]
    ├──> Auth Service
    ├──> Query Service
    │     └──> Retrieval Layer (Vector + Keyword Hybrid)
    │            ├── Vector DB (Qdrant)
    │            ├── BM25 / ElasticSearch
    │            └── Re-ranker (LLM)
    │     └──> LLM Orchestrator (Gemini API)
    └──> Document Ingest Service
          ├── PDF
          ├── Chunker (RecursiveTextSplitter, etc.)
          ├── Embedder (Hugging Face/Open source)
          └── Storage:
                - local
                - Qdrant / FAISS (Chunks)
                - Postgres (Metadata)

